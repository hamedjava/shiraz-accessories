/**
 * =============================================
 * فایل اصلی راه‌اندازی سرور Node.js
 * مسئول اتصال به دیتابیس، بارگذاری تنظیمات env،
 * و اجرای اپلیکیشن Express در پورت مشخص‌شده.
 * ---------------------------------------------
 * این فایل فقط Bootstrapping انجام می‌دهد.
 * =============================================
 */

import dotenv from "dotenv";            // 📦 بارگذاری فایل env برای مدیریت تنظیمات حساس
import app from "./src/app.js";         // 🚀 اپلیکیشن اصلی Express
import connectDB from "./src/config/database.js"; // ⚙️ تابع اتصال به MongoDB

// بارگذاری متغیرهای محیط از فایل .env
dotenv.config();

/* ============================================================
   ⚙️ تنظیمات پایه سیستم از .env یا مقدار پیش‌فرض
============================================================ */
const PORT = process.env.PORT || 3000;
const DB_URI = process.env.DB_URI || "mongodb://127.0.0.1:27017/cleanarch-shop";

/* ============================================================
   🚀 راه‌اندازی سرور و اتصال پایگاه داده
============================================================ */

(async () => {
  try {
    // 🧩 اتصال به پایگاه داده MongoDB با آدرس مشخص‌شده
    await connectDB(DB_URI);
    console.log("✅ اتصال موفق به پایگاه داده برقرار شد");

    // 🛠 اجرای اپلیکیشن روی پورت مشخص شده
    app.listen(PORT, () => {
      console.clear(); // پاک کردن صفحه ترمینال برای نمایش تمیزتر
      console.log("===============================================");
      console.log(" 🏗  Clean Architecture Shop Backend Started");
      console.log("===============================================");
      console.log(`🌐  API URL:        http://localhost:${PORT}/api`);
      console.log(`🗄  Database URI:   ${DB_URI}`);
      console.log(`🚀  Environment:    ${process.env.NODE_ENV || "development"}`);
      console.log("===============================================");
      console.log("📡  Listening for incoming requests...");
      console.log("===============================================");
    });
  } catch (err) {
    // ❌ هندل خطا در اتصال دیتابیس
    console.error("===============================================");
    console.error("❌ خطا در اتصال پایگاه داده MongoDB");
    console.error("جزئیات:", err.message);
    console.error("لطفاً تنظیمات فایل .env یا وضعیت MongoDB را بررسی کنید.");
    console.error("===============================================");
    process.exit(1); // خروج از پروسه برای جلوگیری از اجرای سرور بدون اتصال DB
  }
})();
